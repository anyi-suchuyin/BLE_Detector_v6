# BLE Presence Detector — 完整项目路线图 v3

## 总架构

```
            ┌─────────────────────────────┐
            │       两个物理按钮           │
            │  上按钮=人物  下按钮=场景    │
            └──────┬──────────┬───────────┘
                   │          │
    ┌──────────────▼──┐  ┌───▼──────────────┐
    │  人物捕获引擎    │  │  场景捕获引擎     │
    │  BLE 设备指纹    │  │  WiFi AP + BLE   │
    │  前后15秒快照    │  │  环境信号指纹     │
    └──────┬──────────┘  └───┬──────────────┘
           │                  │
    ┌──────▼──────────────────▼──────────┐
    │         智能分析引擎               │
    │  人物: 跨捕获对比 + 同出同入       │
    │  场景: 指纹匹配 + 自动识别        │
    │  交叉: 跨场景人物关联             │
    └──────┬──────────────────┬──────────┘
           │                  │
    ┌──────▼─────┐    ┌──────▼──────────┐
    │  水墨屏     │    │  Web 配置界面    │
    │  5种显示态  │    │  手机/电脑浏览器  │
    │  头像+提醒  │    │  捕获审查+场景   │
    └────────────┘    └─────────────────┘
```

## 开发步骤总览

### 📋 Step 1: 点亮水墨屏（已完成代码）
验证硬件正常，屏幕能显示文字和图形。

### 📋 Step 2: BLE 扫描验证（已完成代码）
串口打印周围 BLE 设备，观察 MAC、RSSI、广播数据。

### 📋 Step 3: 人物库 + Web 配置（已完成代码）
BLE 扫描 + 设备指纹 + 人物库 + WiFi AP + Web 服务器 + 水墨屏 + 震动。

### 📋 Step 4: 水墨屏主屏 + 按钮捕获（计划完成）
- 4a: 水墨屏状态机（空闲/提醒/捕获 三态）
- 4b: 上按钮人物捕获 + 环形缓冲区
- 4c: 头像上传 + 黑白转换 + 水墨屏显示
- 4d: Web 捕获审查页面

### 📋 Step 5a: 智能人物分析（计划完成）
- 跨捕获对比：多次标记同一人时，找出重复出现的设备
- 同出同入分析：总是同时出现/消失的设备可能属于同一人
- Web 建议提示：一键确认分配

### 📋 Step 5b: 场景识别系统（计划完成）
- 下按钮场景捕获：WiFi + BLE 环境指纹
- 固定场所：办公室/家 → 激活人物出入监控
- 移动场所：通勤/公交 → 激活"曾经相遇"检测
- 自动场景识别：不按按钮也能识别已知场景
- 跨场景人物关联：在多个场景都出现的设备 → 推断关系

### 📋 Step 6: 持续优化
- 低功耗深度睡眠 + RTC 定时唤醒
- SD 卡长期日志 + CSV 导出
- OTA 无线固件升级
- 多台设备协同（远期）

## 水墨屏 5 种显示状态

```
A. 空闲仪表盘  ← 默认状态，显示附近人数+设备+事件
B. 人物提醒    ← 检测到已配置人物，全屏显示头像+名称
C. 人物捕获    ← 上按钮触发，录制 30 秒设备快照
D. 场景捕获    ← 下按钮触发，扫描 WiFi + BLE
E. 场景监控    ← 自动识别场景后，按类型切换监控模式
```

## 双按钮操作逻辑

```
上按钮（BOOT / GPIO0）:
  短按 → 人物捕获模式（记录前后15秒BLE设备）
  长按 → 快速标记最近提醒的设备

下按钮（外接 GPIO6）:
  短按 → 场景捕获模式（扫描WiFi+BLE环境指纹）
  长按 → 手动切换场景监控模式
```

## 推荐实施节奏

```
第 1 周:  Step 1-3（拿到板子后立即可跑，验证全部基础功能）
第 2 周:  Step 4a-4b（水墨屏状态机 + 按钮捕获，设备独立可用）
第 3 周:  Step 4c-4d（头像系统 + Web 审查，体验完整化）
第 4 周:  Step 5a（智能人物分析，减少手动操作）
第 5-6 周: Step 5b（场景识别，系统能力质变）
持续:     Step 6（按需优化）
```

每个 Step 都可以独立编译运行。
前一个 Step 的功能不会因为后一个 Step 的加入而丢失。


# BLE 人员识别器 v4.2 更新汇总

## 本次更新内容 (A + B)

### ✅ A: Web 捕获弹窗修复
- **问题**: `web_server.h` 中的 JavaScript 代码引用了 `#cpm` 和 `#cpmd` 元素，但 HTML 中未定义
- **修复**: 在 `</body></html>` 前添加了捕获详情弹窗结构：
  ```html
  <div class="mo" id="cpm">
    <div class="md" id="cpmd"></div>
  </div>
  ```

### ✅ B: 水墨屏人物提醒全屏显示
新增功能：

1. **全屏提醒界面** (`ui_screens.h`)
   - 黑色背景 + 白色文字，高对比度适合水墨屏
   - 显示人物图标、名称、关系、RSSI/距离估算
   - 自动 8 秒后返回仪表盘

2. **提醒触发逻辑** (`BLE_Detector.ino`)
   - 检测到已配置人物靠近时自动触发
   - 尊重人物设置中的 `alertApproach` 选项
   - **防重复机制**: 同一人 10 秒内不重复提醒

3. **提醒关闭方式**
   - 自动: 8 秒后自动返回仪表盘
   - 手动: 按 BOOT 按钮立即关闭
   - 捕获模式优先: 如果正在捕获，不显示提醒

## 文件变更清单

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `web_server.h` | 修复 | 添加捕获详情弹窗 HTML |
| `ui_screens.h` | 新增 | 添加人物提醒全屏显示功能 |
| `BLE_Detector.ino` | 修改 | 集成提醒触发逻辑和防重复机制 |

## 提醒界面布局 (200×200)

```
┌─────────────────────┐
│                     │
│      [人物图标]      │  y=30
│         👤          │
│                     │
│      张三           │  y=90 (名称)
│      [家人]         │  y=110 (关系)
│                     │
│   -45dBm  1-3m      │  y=135 (距离)
│                     │
│   Detected nearby   │  y=180 (提示)
│                     │
└─────────────────────┘
```

## 使用说明

1. **配置人物**: 通过 Web 界面添加人物，勾选"靠近时提醒"
2. **自动检测**: 当该人物的设备靠近时，水墨屏自动显示全屏提醒
3. **关闭提醒**: 
   - 等待 8 秒自动关闭
   - 或按 BOOT 按钮立即关闭
4. **防重复**: 同一人 10 秒内不会重复触发提醒

## 下一步建议 (可选)

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P1 | 头像显示 | 为人物添加头像，提醒时显示黑白头像 |
| P2 | 离开提醒 | 支持 `alertDepart` 选项，离开时震动提醒 |
| P3 | 场景识别 | 记录 WiFi+BLE 环境指纹，自动识别场景 |
| P4 | 智能分析 | 跨捕获对比，找出同一个人的多个设备 |


# BLE 设备识别器 v4.3 - 更新汇总

## 本次更新内容

### 1. 精简串口输出
- **频率**: 每5轮扫描或30秒输出一次（取先达到的）
- **触发条件**: 达到间隔 **或** 发现新设备时立即输出
- **格式**:
  ```
  [BLE] === 扫描汇总 #50 (30s) ===
  附近3个: Apple iPhone(-45) Xiaomi Watch(-62) Samsung Buds(-78) +2更多
  历史统计: 累计发现12个设备 | 本轮 新+1 离开-0
  ```

### 2. SD卡设备发现历史
- **文件**: `/ble_data/devices_history.csv`
- **字段**: 指纹,名称,厂商,MAC,首次发现,最后发现,次数,平均RSSI,最强,最弱
- **容量**: 最多200个不同设备
- **保存**: 每5分钟自动保存，启动时自动加载

### 3. 修复的WiFi连接问题
- 添加 `esp_wifi_restore()` 清除缓存配置
- 禁用 `WiFi.persistent(false)` 避免历史干扰
- 添加WiFi扫描确认AP存在
- 详细错误码诊断（36=密码错误，201=未找到AP）

## 文件清单

| 文件 | 说明 | 操作 |
|------|------|------|
| `ble_scanner.h` | BLE扫描 + 精简输出 + 设备统计 | 替换 |
| `person_db.h` | 设备历史存储功能 | 替换 |
| `BLE_Detector.ino` | 集成设备历史加载/保存 | 替换 |
| `web_server.h` | WiFi连接修复v3 | 替换（如需要）|

## 使用建议

### 查看设备统计
在代码中调用：
```cpp
db_print_device_history_stats();  // 打印Top 5设备
```

### 导出CSV分析
1. 取出SD卡插入电脑
2. 打开 `/ble_data/devices_history.csv`
3. 用Excel分析设备出现频率和信号强度

### CSV格式示例
```csv
fingerprint,name,company,mac,firstSeen,lastSeen,discoveryCount,avgRssi,minRssi,maxRssi
A1B2C3D4,"iPhone","Apple","AA:BB:CC:DD:EE:FF",1234567890,1234567990,15,-52,-45,-78
E5F67890,"Mi Band","Xiaomi","11:22:33:44:55:66",1234567900,1234568000,8,-68,-62,-75
```


# BLE 设备识别器 v4.3.2 - 编译修复完成

## 修复内容

### 1. 编译错误完全修复
**问题**: person_db.h 中的字符串引号导致编译失败（'s' was not declared）

**根本原因**: snprintf 中使用 \"%s\" 格式时，字符串参数处理有问题

**解决**: 
- 完全重写 `db_save_device_history()`，使用 `f.write(0x22)` 输出双引号（ASCII码方式）
- 完全重写 `db_load_device_history()`，使用 String 的 indexOf 和 substring 解析CSV
- 使用 `Serial.print` 分多行输出，避免复杂格式化
- 删除所有可能有问题的 snprintf 调用

### 2. 靠近/离开检测逻辑（保持v4.3.1）
- **靠近**: `wasNearby=false` → `isNearby=true`（从无到有）
- **离开**: `wasNearby=true` → `isNearby=false`（从有到无）
- 滞后区间（-80 ~ -70dBm）保持状态防止抖动

## 最终文件清单

| 文件 | 修改内容 | 状态 |
|------|---------|------|
| `ble_scanner.h` | 靠近/离开边缘检测 + 精简输出 | ✅ 已完成 |
| `person_db.h` | 设备历史存储（编译修复版） | ✅ 已完成 |
| `BLE_Detector.ino` | 集成设备历史加载/保存 | ✅ 已完成 |

## 使用说明

### 1. 替换所有文件
- `ble_scanner.h` → 项目目录
- `person_db.h` → 项目目录  
- `BLE_Detector.ino` → 项目目录（如果包含历史加载/保存代码）

### 2. 编译上传
应该没有任何编译错误了。

### 3. 观察输出
```
[BLE] === 扫描汇总 #50 (30s) ===
附近3个: Apple iPhone(-45) Xiaomi Watch(-62) Samsung Buds(-78) +2更多
历史统计: 累计发现12个设备 | 本轮 新+0 靠近+2 离开-1
```

### 4. SD卡文件
- 路径: `/ble_data/devices_history.csv`
- 格式: CSV，可用Excel打开
- 自动保存: 每5分钟

## 如果还有问题

请提供：
1. 完整的错误信息
2. 出错的文件名和行号
3. 相关代码片段

我会立即继续修复！

