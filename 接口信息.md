/**
 * ESP32-S3-ePaper-1.54接口总结：
 */

// 1. e-Paper (电子纸屏幕 - SPI接口)
 * 接线：
 *   - CS:   IO11 (EPD_CS)
 *   - DC:   IO10 (EPD_D/C)
 *   - RST:  IO9  (EPD_RST)
 *   - BUSY: IO8  (EPD_BUSY)
 *   - MOSI: IO13 (EPD_SDI)
 *   - SCK:  IO12 (EPD_SCLK)
 *   - PWR_EN: IO6 (EPD3V3_EN)

// 3. SD Card (SD卡接口)
 * 接线：
 *   - CLK:  IO39 (SD_CLK)
 *   - MISO: IO40 (SD_MISO)
 *   - MOSI: IO41 (SD_MOSI)

// 4. I2S (音频接口)
 * 接线：
 *   - MCLK: IO14 (I2S_MCLK)
 *   - SCLK: IO15 (I2S_SCLK)
 *   - ASDOUT: IO16 (I2S_ASDOUT)
 *   - LRCK: IO38 (I2S_LRCK)
 *   - DSDIN: IO45 (I2S_DSDIN)

// 5. UART & USB (串口与USB)
 * 接线：
 *   - TXD:  IO43
 *   - RXD:  IO44
 *   - USB_D-: IO19 (U N)
 *   - USB_D+: IO20 (U P)

// 6. RTC (实时时钟)
 * 接线：
 *   - INT:  IO5  (RTC INT)
 *   - SDA:  IO47 (RTC SDA, 与触摸屏SDA复用)
 *   - SCL:  IO48 (RTC SCL, 与触摸屏SCL复用)

// 7. Other (其他控制)
 * 接线：
 *   - BOOT0: IO0
 *   - BAT_ADC: IO4
 *   - PA_EN: IO18 / IO42
 *   - PA_CTRL: IO46
 */